# 活字排版工坊

一个纯前端本地应用，用来维护字模库、在网格版面中落字、检查字模数量是否超用、保存草稿并导出PNG预览图。

支持开印锁定：作品名为空或存在超量字模时锁定失败并就地提示；锁定后落字、换字、字模增减、纸张方向与网格调整均不可用，仅可导出或解除锁定。草稿会保存锁定状态，载入时一并恢复，全部数据保存在浏览器本地。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。
